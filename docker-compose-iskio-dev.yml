services:
  front-app:
    build: .
    container_name: app-front
    env_file:
      - .env
    expose:
      - "9443"
    networks:
      iskio_net_dev:
        ipv4_address: 172.16.148.11
    restart: unless-stopped

networks:
  iskio_net_dev:
    external: true
    name: iskio_net_dev